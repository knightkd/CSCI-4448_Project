<%= form_for(@partner_list) do |f| %>
  <% if @partner_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@partner_list.errors.count, "error") %> prohibited this partner_list from being saved:</h2>

      <ul>
      <% @partner_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :organization_id %><br>
    <%= f.text_field :organization_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
